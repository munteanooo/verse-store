Verse Store was installed in namespace {{ .Release.Namespace }}.
{{- if .Values.ingress.enabled }}
App:      https://{{ .Values.ingress.app.host }}
Keycloak: https://{{ .Values.ingress.keycloak.host }}
{{- if and .Values.mailpit.enabled .Values.ingress.mailpit.enabled }}
Mailpit:  https://{{ .Values.ingress.mailpit.host }}
{{- end }}
{{- else }}
Ingress is disabled. Enable it and provide an existing TLS Secret for local HTTPS.
{{- end }}
