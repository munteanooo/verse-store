{{- if .Values.ingress.enabled }}
{{- $host := required "ingress.app.host is required when ingress is enabled" .Values.ingress.app.host }}
{{- $tlsSecret := required "ingress.tlsSecretName is required when ingress is enabled" .Values.ingress.tlsSecretName }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "verse-store.fullname" . }}-app
  labels:
    {{- include "verse-store.labels" . | nindent 4 }}
    app.kubernetes.io/component: app
  annotations:
    {{- toYaml .Values.ingress.annotations | nindent 4 }}
spec:
  ingressClassName: {{ required "ingress.className is required when ingress is enabled" .Values.ingress.className | quote }}
  tls:
    - hosts: [{{ $host | quote }}]
      secretName: {{ $tlsSecret | quote }}
  rules:
    - host: {{ $host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "verse-store.fullname" . }}
                port: {name: http}
{{- end }}
