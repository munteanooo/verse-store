global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: verse-store
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: [app:9090]
        labels:
          application: verse-store
          environment: docker-local
