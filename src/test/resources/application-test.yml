spring:
  jpa:
    show-sql: false
  flyway:
    clean-disabled: false

KEYCLOAK_CLIENT_SECRET: test-only-client-secret
MINIO_ROOT_USER: test-only-access-key
MINIO_ROOT_PASSWORD: test-only-secret-key
